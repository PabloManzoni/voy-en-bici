# Copiá este archivo a `.env.local`.
# La key de Gemini es la misma que usa que-comer (Google AI Studio).
# Sin key la app funciona igual: usa explicaciones de plantilla en vez de IA.
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-2.5-flash
